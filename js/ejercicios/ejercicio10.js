const num = parseInt(prompt("ingrese un numero"));
if (num % 2 === 0) {
  document.write("el numero ingresado " + num + " es divisible por 2 ");
} else {
  document.write("el numero ingresado " + num + " no es divisible entre 2");
}
